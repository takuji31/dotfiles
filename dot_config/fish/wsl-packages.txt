# WSL Ubuntu用パッケージリスト
# フォーマット: package_name:check_command:install_method
# install_method: apt (デフォルト), brew, snap
# install_methodを省略した場合はaptが使用されます
# コメント行（#で始まる行）と空行は無視されます

# 必須レベル
build-essential:gcc
git:git
tig:tig
curl:curl
perl:perl
vim:vim
tmux:tmux

# よく使うツール
peco:peco:brew
ghq:ghq:brew
graphviz:dot
jq:jq
tree:tree
unzip:unzip
zip:zip
ca-certificates:update-ca-certificates

# テキスト処理
gawk:awk
sed:sed
grep:grep
findutils:find
coreutils:sort

# オプション（便利ツール）
htop:htop
wget:wget

# Modern CLI tools (brew)
ripgrep:rg:brew
fd:fd:brew
bat:bat:brew
starship:starship:brew

# 追加ツール（コメントアウト例）
# eza:eza:brew
